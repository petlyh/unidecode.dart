name: unidecode
description: ASCII transliterations of Unicode text. Dart port of the unidecode Python library.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
